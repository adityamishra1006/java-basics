package javaDevelopment.Day4;

public class packages {

    // We create the structure of the project
    // We have to import the package here
    // We can import the package using import keyword
    // If we are using * during import then it will only imprt the files not the folder in that package
    
}
