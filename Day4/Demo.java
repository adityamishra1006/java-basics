package javaDevelopment.Day4;

import javaDevelopment.Day4.*;

public class Demo {
    public static void main(String[] args) {
        // A obj = new A();
        // System.out.println(obj.marks);

        B obj1 = new B();
        System.out.println(obj1.marks);
    }    
}

// Public : If we want to access anything from any file in the package or outside the package we use public.

// Private : It is used only in the same class.

// Default: Used when anything can be accessed in the same package.

// Protected : it is accessible within the same package and subclasses in other packages.

